@model IEnumerable<Group1APIProject.Models.RecipeFavorite>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserData)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Result)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserData.UserDataID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Result.ResultId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RecipeFavoriteId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RecipeFavoriteId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RecipeFavoriteId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
